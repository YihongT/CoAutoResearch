name: Paper contracts

on:
  pull_request:
    paths:
      - "templates/default/ui/v2_*.py"
      - "templates/default/schemas/**"
      - "templates/default/research_trajectory/**/*.example.json"
      - "tests/paper_contracts/**"
      - ".github/patches/paper-contracts/**"
      - ".github/workflows/paper-contracts.yml"
  push:
    branches: [main, paper-contracts-validation]
    paths:
      - "templates/default/ui/v2_*.py"
      - "templates/default/schemas/**"
      - "templates/default/research_trajectory/**/*.example.json"
      - "tests/paper_contracts/**"
      - ".github/patches/paper-contracts/**"
      - ".github/workflows/paper-contracts.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Apply validated production patches
        run: |
          git apply --check .github/patches/paper-contracts/0001-tighten-evidence-admission.patch
          git apply .github/patches/paper-contracts/0001-tighten-evidence-admission.patch
          git apply --check .github/patches/paper-contracts/0002-validate-critical-path-target.patch
          git apply .github/patches/paper-contracts/0002-validate-critical-path-target.patch
      - name: Run paper-facing contract tests
        run: python -m unittest discover -s tests/paper_contracts -p 'test_*.py' -v
