# CoAutoResearch Claude Code Entry Point

Claude Code should treat `AGENTS.md` as the canonical project instruction file.

Before substantive work:

1. Read `AGENTS.md`.
2. Follow the repository map, workflow, reviewer gates, and file ownership rules there.
3. Prefer `instructions/` for detailed protocol decisions.

Do not treat this file as a replacement for `AGENTS.md`; it is only a Claude Code bridge.
